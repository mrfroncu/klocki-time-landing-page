# Bez klucza "world:" — ta instancja BlueMapa niczego nie renderuje, tylko
# rejestruje mapę do webapp/webservera i serwuje gotowe kafelki ze storage
# "sql" (patrz storages/sql.conf), które renderuje plugin na serwerze MC.
name: "Overworld"
storage: "sql"
sorting: 0
